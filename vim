vim查找：
格式：/查找的内容
如：/aaa
如果查找多个
n跳到下一个，shift+n跳到上一个
替换：
格式： :%s/原字符串/新字符串/g（全部替换）
       :%s/原字符串/新字符串/gc（单个替换，替换之前要确认）
示例：
      :%s/aaa/ooo/g（将所以aaa替换为ooo)
      :%s/aaa/ooo/gc（将所以aaa替换为ooo，要逐个确认，按y表示替换，n表示不替换）
